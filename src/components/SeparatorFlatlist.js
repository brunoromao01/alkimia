import React from 'react'
import { View } from 'react-native'
import estilo from '../estilo'
export default props => {
    return (
        <View style={{ borderWidth: 1, borderColor: 'white', borderRadius: 10, }}></View>
    )
}
